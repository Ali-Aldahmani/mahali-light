// Re-exports the socket store for ergonomic imports:
//   import { useSocketStore } from 'src/socket';
// (The full socket lifecycle lives in src/store/socketStore.js.)

export { useSocketStore } from '../store/socketStore.js';
